N = 100000;
x = randn(N,1);
y = sqrt(3)*x + 2;

mean(y)
var(y)
[counts, x_values] = hist(y,100);
breite = diff(x_values(1:2));
bar(x_values, counts/N)
% histogram(y,100, 'Normalization','probability')
% stem(x_values, counts/N)